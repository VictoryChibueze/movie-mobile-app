import { StyleSheet, View, Text } from "react-native";

const Saved = () => {
  return (
    <View>
      <Text>Saved</Text>
    </View>
  );
};

export default Saved;

const styles = StyleSheet.create({});
