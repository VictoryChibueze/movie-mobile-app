import { StyleSheet, View, Text } from "react-native";

const Saved = () => {
  return (
    <View className="flex-1 bg-[#000]">
      <Text className="text-white mt-10">yooo</Text>
    </View>
  );
};

export default Saved;

const styles = StyleSheet.create({});
